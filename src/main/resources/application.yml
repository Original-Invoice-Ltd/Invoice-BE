spring:
  application:
    name: Invoice-BE
  profiles:
    active: prod

server:
  port: 8089

# OAuth and app configuration is in profile-specific files
# application-dev.yml for development
# application-prod.yml for production