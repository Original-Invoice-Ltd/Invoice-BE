spring:
  application:
    name: Invoice-BE
  profiles:
    active: prod 

# Pusher Configuration
pusher:
  app-id: ${PUSHER_APP_ID:your-app-id}
  key: ${PUSHER_KEY:your-key}
  secret: ${PUSHER_SECRET:your-secret}
  cluster: ${PUSHER_CLUSTER:us2}
  encrypted: true

server:
  port: 8089
